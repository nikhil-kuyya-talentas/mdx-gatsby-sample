backend:
  name: git-gateway
  branch: master

media_folder: "static/admin/images"

collections:
  - name: "carousel"
    label: "Carousel"
    folder: "static/admin/content/carousel"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Layout", name: "layout", widget: "hidden", default: "blog"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Image", name: "image", widget: "image"}